[pytest]
# src/ modules import each other by bare name (import config, extract, ...), so put src on
# the import path. Tests are hermetic: they build synthetic fixtures and never hit EDGAR.
pythonpath = src
testpaths = tests
addopts = -q
